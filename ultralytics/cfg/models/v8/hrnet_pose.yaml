# Ultralytics HRNet Pose (center-keypoint) config
# This config is consumed by ultralytics.models.hrnet.HRNetPoseModel.

width: 32
hrnet_pose: true  # lets guess_model_task(cfg_dict) return pose without a YOLO "head"
